package com.company;

import java.util.ArrayList;

public class Main {

    public static void main(String[] args) {
        /*int repetitions = 10;
        Long gtfSum = 0L;
        Long pairSum = 0L;
        Long writingSum = 0L;
        Long totalSum = 0L;
        for(int i = 0; i < repetitions; i++){
            ArrayList<Long> r = Runner.main(args);
            gtfSum += r.get(0);
            pairSum += r.get(1);
            writingSum += r.get(2);
            totalSum += r.get(3);
        }

        System.out.println((gtfSum/(double)repetitions)+" & "+(pairSum/(double) repetitions)+" & "+(writingSum/(double)repetitions+" & "+(totalSum/(double)repetitions)));*/
    }
}
